Environment
  ✓ Pkgtools   installpkg, upgradepkg, removepkg present
  ! Tools      missing: diff (new-config diffs)
  ✓ Config     repos and mirrors parse and cross-check cleanly

Setup
  ✓ Mirror     https://mirror.example/slackware64-current/
  ✓ Repos      3 configured, priorities distinct
  ! Repo trust 1 repo(s) frozen: shady
             shady: bad signature
  · GPG keys   cannot verify without root
  ! Metadata   cached for 2/3 repos
  ! Tag prio   2 installed package(s) from 1 unranked tag(s) — kept for safety, never upgraded
             _rtz: foo, bar — add `100  rtz  _rtz` to `repos`, or removepkg and install from a repo

Installed
  ✓ Packages   2173
  · By source  slackware 1887, conraid ?, SBo 286
  ·            ? = no metadata yet (conraid) — run `slacker update`, or check the repo URL

Online
  ✗ Reachable  slackware unreachable: timed out

slacker is configured. Recommended next steps, in order:
  → slacker update
  → slacker install-new
  → slacker upgrade-all
  ! the mirror did not respond — check your network or the active line in `mirrors`
